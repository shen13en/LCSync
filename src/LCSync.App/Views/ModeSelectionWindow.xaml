<Window x:Class="LCSync.Views.ModeSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LCSync - 实验中心联屏系统"
        Width="1000"
        Height="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FFFFFF">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,60,0,0">
            <TextBlock Text="LCSync" 
                       FontSize="48" 
                       FontWeight="800" 
                       Foreground="#1E293B" 
                       HorizontalAlignment="Center"
                       FontFamily="Segoe UI"/>
            
            <TextBlock Text="实验中心联屏系统" 
                       FontSize="18" 
                       Foreground="#64748B" 
                       HorizontalAlignment="Center"
                       Margin="0,12,0,0"
                       FontWeight="400"/>
            
            <TextBlock Text="高效 · 低带宽 · 云桌面优化" 
                       FontSize="13" 
                       Foreground="#94A3B8" 
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0"/>
        </StackPanel>
        
        <Grid Grid.Row="1" Margin="80,40,80,60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" 
                    Background="#F8FAFC" 
                    BorderBrush="#E2E8F0" 
                    BorderThickness="1" 
                    CornerRadius="16"
                    Padding="40">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" HorizontalAlignment="Center">
                        <TextBlock Text="👨‍🏫" FontSize="48" HorizontalAlignment="Center"/>
                        <TextBlock Text="教师模式" 
                                   FontSize="22" 
                                   FontWeight="700" 
                                   Foreground="#1E293B" 
                                   HorizontalAlignment="Center"
                                   Margin="0,16,0,0"/>
                    </StackPanel>
                    
                    <TextBlock Grid.Row="1" 
                               Text="共享您的屏幕给所有学生观看，支持多种画质选择，实时显示连接状态和带宽使用情况。"
                               FontSize="14" 
                               Foreground="#64748B" 
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,30"/>
                    
                    <Button Grid.Row="2" 
                            Content="进入" 
                            Command="{Binding OpenTeacherModeCommand}"
                            Background="#3B82F6"
                            Foreground="White"
                            FontSize="15"
                            FontWeight="600"
                            Padding="0,14"
                            BorderThickness="0"
                            Cursor="Hand"
                            HorizontalAlignment="Stretch">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="8" 
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
            
            <Border Grid.Column="2" 
                    Background="#F8FAFC" 
                    BorderBrush="#E2E8F0" 
                    BorderThickness="1" 
                    CornerRadius="16"
                    Padding="40">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" HorizontalAlignment="Center">
                        <TextBlock Text="👨‍💻" FontSize="48" HorizontalAlignment="Center"/>
                        <TextBlock Text="学生模式" 
                                   FontSize="22" 
                                   FontWeight="700" 
                                   Foreground="#1E293B" 
                                   HorizontalAlignment="Center"
                                   Margin="0,16,0,0"/>
                    </StackPanel>
                    
                    <TextBlock Grid.Row="1" 
                               Text="输入教师IP地址即可观看屏幕共享，无需复杂设置，即连即用。"
                               FontSize="14" 
                               Foreground="#64748B" 
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,30"/>
                    
                    <Button Grid.Row="2" 
                            Content="进入" 
                            Command="{Binding OpenStudentModeCommand}"
                            Background="#10B981"
                            Foreground="White"
                            FontSize="15"
                            FontWeight="600"
                            Padding="0,14"
                            BorderThickness="0"
                            Cursor="Hand"
                            HorizontalAlignment="Stretch">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="8" 
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
        </Grid>
        
        <TextBlock Grid.Row="1" 
                   Text="版本 1.2.0 · LCSync 实验室联屏系统"
                   FontSize="12" 
                   Foreground="#94A3B8" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,20"/>
    </Grid>
</Window>
